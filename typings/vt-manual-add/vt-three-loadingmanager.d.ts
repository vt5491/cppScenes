// the def for LoadingManager is already in the base three.d.ts file, thus I don't need this.
declare namespace THREE {
  export class LoadingManager {
    //constructor(name: string, times: any[], values: any[], interpolation: InterpolationModes);
    constructor()

//    material : any
//    render()
//    renderWithMirror(otherMirror : Mirror)
  }
}
